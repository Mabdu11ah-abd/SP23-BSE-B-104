<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    body {
      background-color: #1a1a1a;
      color: #f8f9fa;
    }
    .card {
      background-color: #2c2c2c;
      border: none;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .list-group-item {
      background-color: #343a40;
      border-color: #454d55;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="card">
      <div class="card-body">
        <h1 class="card-title mb-4">Order Details</h1>
        <div class="row">
          <div class="col-md-6">
            <p><strong>Order ID:</strong> <span class="badge bg-secondary"><%= order._id %></span></p>
            <p><strong>User:</strong> <%= order.user.name %> (<%= order.user.email %>)</p>
            <p><strong>Total Price:</strong> <span class="text-success">$<%= order.totalPrice.toFixed(2) %></span></p>
          </div>

          <div class="col-md-6">
            <p><strong>Status:</strong> <span class="badge bg-primary"><%= order.status %></span></p>
            <p><strong>Order Date:</strong> <%= new Date(order.orderDate).toLocaleDateString() %> </p>

            <p><strong>Address: </strong><%= order.address %> <span class="badge bg-primary"> <i class="fa-solid fa-house" style="color: #ece8f8;"></i></span></p>
          </div>
          
        </div>

        <h3 class="mt-4 mb-3">Products</h3>
        <ul class="list-group">
          <% order.products.forEach(item => { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <div>
                <strong><%= item.product.title %></strong>
                <span class="ms-2 badge bg-info">Qty: <%= item.quantity %></span>
              </div>
              <span class="badge bg-success">$<%= item.price.toFixed(2) %></span>
            </li>
          <% }) %>
        </ul>
        
        <div class="mt-4">
          <a href="/orders" class="btn btn-outline-light">
            <i class="bi bi-arrow-left me-2"></i>Back to Orders
          </a>
        </div>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

